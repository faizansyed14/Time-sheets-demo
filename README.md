# Timesheets tool demo

Frontend-only demo (mock API in Vite). No backend or database.

## Run

```bash
cd frontend
npm install
npm run dev
```

See [frontend/README.md](frontend/README.md) for demo login accounts and features.
