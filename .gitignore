# Dependencies & build outputs
node_modules/
dist/

# Demo persistence (mock API)
**/.demo-store.json

# Secrets & local env
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
*.local

# TypeScript incremental build info
*.tsbuildinfo

# App lives in frontend/ — ignore accidental root npm init
/package-lock.json
